package theme

import androidx.compose.ui.graphics.Color

// Click Brand Colors - Ultra-minimal palette inspired by Linear
object ClickColors {
    // Primary - Refined indigo
    val Primary = Color(0xFF5E6AD2)
    val PrimaryVariant = Color(0xFF4C5FD5)
    val PrimaryLight = Color(0xFF9EA6F0)

    // Secondary - Subtle purple accent
    val Secondary = Color(0xFF8B5CF6)
    val SecondaryVariant = Color(0xFF7C3AED)
    val SecondaryLight = Color(0xFFA78BFA)

    // Accent - Muted blue
    val Accent = Color(0xFF5E6AD2)
    val AccentLight = Color(0xFFBBBFF7)

    // Backgrounds - Ultra minimal
    val Background = Color(0xFFFCFCFC)
    val Surface = Color(0xFFFFFFFF)
    val SurfaceVariant = Color(0xFFF7F8F9)
    val SurfaceElevated = Color(0xFFFAFAFB)

    // Text - Refined hierarchy
    val TextPrimary = Color(0xFF0D0E12)
    val TextSecondary = Color(0xFF616469)
    val TextTertiary = Color(0xFF9EA0A5)
    val TextQuaternary = Color(0xFFCACBCF)

    // Borders & Dividers - Barely visible
    val Border = Color(0xFFEAEBEC)
    val BorderHover = Color(0xFFD4D5D8)
    val Divider = Color(0xFFEEEFF0)

    // Functional - Muted
    val Success = Color(0xFF10B981)
    val Warning = Color(0xFFF59E0B)
    val Error = Color(0xFFEF4444)
    val Info = Color(0xFF3B82F6)

    // Interactive states
    val Hover = Color(0xFFF5F6F7)
    val Active = Color(0xFFEDEEF0)

    // Map pins
    val MapPin = Color(0xFF5E6AD2)
    val MapPinCluster = Color(0xFF9EA6F0)

    // Dark theme - Pure minimal
    val DarkBackground = Color(0xFF0D0E12)
    val DarkSurface = Color(0xFF16181D)
    val DarkSurfaceVariant = Color(0xFF1C1E24)
    val DarkSurfaceElevated = Color(0xFF23252B)
    val DarkBorder = Color(0xFF26282E)
    val DarkTextPrimary = Color(0xFFF0F0F1)
    val DarkTextSecondary = Color(0xFFB4B5B9)
    val DarkTextTertiary = Color(0xFF696A6F)
}
