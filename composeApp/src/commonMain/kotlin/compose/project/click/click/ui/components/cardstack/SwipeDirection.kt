package compose.project.click.click.ui.components.cardstack // pragma: allowlist secret

/**
 * Vendored from Hukumister LazyCardStack (Apache-2.0 / Compose card-stack lineage).
 * Kept internal so Home Polaroids can use proven swipe physics without a stale Maven artifact
 * targeting Compose 1.5.
 */
enum class SwipeDirection {
    Left,
    Right,
    Up,
    Down,
}
