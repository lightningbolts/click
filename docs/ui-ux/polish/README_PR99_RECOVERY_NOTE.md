# First recovery slice

The first implementation slice intentionally changes only contracts that can be reasoned about without subjective visual redesign: the verified Me-tab avatar fix, event-hub RSVP messaging, and explicit check-in failure semantics. The remaining state-machine and UI work is tracked in `PR99_RECOVERY_STATUS.md` and must be implemented in isolated commits.
